[workspace]
members = ["shared", "template", "year-2024"]
resolver = "2"

[workspace.dependencies]
shared = { path = "shared" }
regex = { version = "1.11", default-features = true, features = [
    "std",
    "perf",
] }
